[project]
name = "southfarm-publisher-worker"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
southfarm-test-cleanup = "southfarm_publisher.cleanup_cli:main"

[tool.unittest]
start-directory = "tests"
